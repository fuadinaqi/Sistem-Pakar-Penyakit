<!DOCTYPE html>
<html>
<% include ./partials/head %>
<div class="container-fluid">
  <body>

    <h1>Terindikasi implikasi Obat!</h1>
    <h3>Untuk anda yang terjangkit penyakit <%= obats[0].Diagnosas[0].namaPenyakit %>
    pilihan obat anda : </h3>
    <ol>
      <% obats.forEach(function(obat) { %>
        <li><%= obat.namaObat %></li>
      <%})%>
    </ol>
    <h3>Warning!! </h3>
    <% obats.forEach(function(obat) {%>
      <% if(obat.dataValues.namaImplikasi) {%>
      <h4>obat <%= obat.namaObat %> terimplikasi dengan obat <%= obat.dataValues.namaImplikasi %></h4>
      <%}%>
    <%})%>
    <h5>Silahkan memilih <a href="/patients/sakit/<%= obats[0].Diagnosas[0].id %>/obat">ulang</a> obat yang tidak terimplikasi </h5>

  </body>
</div>
</html>
